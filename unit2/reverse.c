#include <cs50.h>1. #include <stdio.h>2. #include <string.h>3. 4. int main(void)5. {6.    // Declarar variable7.    string name = GetString();8.   9.    // Si primera letra es minuscula lo hace mayuscula, si ya lo esta lo imprime10.    if (name[0] >= 'a' && name[0] <= 'z')11.    {12.        printf("%c", name[0] - ('a' - 'A'));    13.    }14.    else15.    {16.        printf("%c", name[0]);17.    }18.   19.    // Recorre cada caracter del nombre(s) y busca un espacio    20.    for (int i = 0, n = strlen(name); i < n; i++)21.    {22.    // Encuentra un espacio, convierte la siguiente letra a mayusucla si no esta23.        if (name[i] == ' ')24.        {25.            if (name[i + 1] >= 'a' && name[i + 1] <= 'z')26.            {27.                printf("%c", name[i + 1] - ('a' - 'A'));28.            }29.            else30.            {31.                printf("%c", name[i + 1]);  32.            }33.        }34.    }35.    // Imprime espacio36.    printf("\n");37. 